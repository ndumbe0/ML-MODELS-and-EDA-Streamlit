version: '3.8'

services:
  streamlit-app:
    build: .
    container_name: telco-churn-app
    ports:
      - "127.0.0.1:8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    env_file:
      - .env
    restart: unless-stopped
