version: '3.8'

services:
  streamlit-app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - GOOGLE_AI_API_KEY=${GOOGLE_AI_API_KEY}
